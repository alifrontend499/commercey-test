// ACTION CREATORS
import { } from "redux/actions/constants/action-types";

// initial state
const initialState = {
    // commonToken: '',
    // currentUser: null,
    isUserAuthenticated: true
};

function authReducer(state = initialState, action) {
    // if (action.type === EXAMPLE_ACTION) {
    //     return {
    //         ...state,
    //         data: data,
    //     }
    // }

    return state
}

export default authReducer;